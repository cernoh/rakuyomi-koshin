---
phase: 01-backend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/migrations/20260628000001_create_tracking_tables.sql
  - backend/shared/src/track/mod.rs
  - backend/shared/src/track/types.rs
  - backend/shared/src/lib.rs
  - backend/server/src/track/state.rs
  - backend/server/src/state.rs
autonomous: true
requirements: [DB-01, DB-02, DB-03, DB-04, QR-06]
user_setup: []

must_haves:
  truths:
    - "Migration creates `track` table with all 16 columns including UNIQUE(manga_source_id, manga_id, tracker_id)"
    - "Migration creates `tracker_auth` table with UNIQUE(tracker_id) for OAuth token storage"
    - "Migration includes indices on (tracker_id, manga_id) and (state) for PKCE lookups"
    - "STRICT mode used on both tables following existing migration pattern"
    - "shared::track::types module exports TrackerService enum, TrackEntry struct, TrackStatus enum, SyncDirection enum, AuthToken struct, PkceSessionData struct"
    - "All domain types derive Serialize, Deserialize, Clone, Debug — matching existing model.rs newtype pattern"
    - "TrackState with Arc<Mutex<HashMap<String, PkceSession>>> exists in server state"
    - "FromRef<State> for TrackState is implemented allowing route handlers to extract TrackState"
    - "PkceSession stores code_verifier: String, tracker_id: String, created_at: Instant"
  artifacts:
    - path: "backend/shared/migrations/20260628000001_create_tracking_tables.sql"
      provides: "SQLite schema for track and tracker_auth tables"
      min_lines: 65
    - path: "backend/shared/src/track/types.rs"
      provides: "Domain types for tracking — TrackerService, TrackEntry, TrackStatus, SyncDirection, AuthToken, PkceSessionData"
      min_lines: 100
    - path: "backend/server/src/track/state.rs"
      provides: "In-memory PKCE session state — TrackState, PkceSession structs"
      min_lines: 35
  key_links:
    - from: "backend/shared/src/lib.rs"
      to: "backend/shared/src/track/mod.rs"
      via: "pub mod track;"
      pattern: "pub mod track;"
    - from: "backend/server/src/state.rs"
      to: "backend/server/src/track/state.rs"
      via: "TrackState field + FromRef<State> for TrackState"
      pattern: "track_state"

---

<objective>
Create the foundational data layer for Phase 1: SQLite schema (migration + tables), shared Rust domain types, and in-memory PKCE state infrastructure.

Purpose: Every subsequent plan depends on these artifacts — the migration provides the tables that store auth tokens, the types module provides the shared vocabulary (TrackerService, TrackEntry, etc.), and TrackState provides the in-memory PKCE session store needed by the MAL OAuth flow.

Output:
- `backend/shared/migrations/20260628000001_create_tracking_tables.sql` — migration file
- `backend/shared/src/track/mod.rs` + `types.rs` — shared domain types
- `backend/server/src/track/state.rs` — in-memory PKCE session management
- Edits to `backend/shared/src/lib.rs` and `backend/server/src/state.rs` — wiring
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-backend-foundation-database-schema-auth-qr/01-CONTEXT.md
@.planning/phases/01-backend-foundation-database-schema-auth-qr/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/INTEGRATIONS.md

# Pattern references for migration and newtype style
@backend/shared/migrations/20260305005317_create_playlists_table.sql
@backend/shared/migrations/20250601202635_create_manga_state_table.sql
@backend/shared/src/model.rs:1-25
@backend/server/src/state.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create tracking tables migration</name>
  <files>
    backend/shared/migrations/20260628000001_create_tracking_tables.sql
  </files>
  <action>
    Create a single sqlx migration file creating both the `track` and `tracker_auth` tables with indices.

    Use naming convention `YYYYMMDDHHMMSS_create_tracking_tables.sql` matching existing timestamp-prefixed migration files (e.g., `20260305005317_create_playlists_table.sql`).

    **`track` table** (per D-04 — DB Schema Design):
    - `id INTEGER PRIMARY KEY AUTOINCREMENT`
    - `manga_source_id TEXT NOT NULL`
    - `manga_id TEXT NOT NULL`
    - `tracker_id TEXT NOT NULL`
    - `remote_id TEXT`
    - `library_id TEXT`
    - `title TEXT`
    - `last_chapter_read INTEGER DEFAULT 0`
    - `total_chapters INTEGER`
    - `status TEXT`
    - `score INTEGER`
    - `start_date TEXT`
    - `finish_date TEXT`
    - `tracking_url TEXT`
    - `private INTEGER DEFAULT 0`
    - `updated_at TEXT NOT NULL DEFAULT (datetime('now'))`
    - `UNIQUE(manga_source_id, manga_id, tracker_id)`
    - Use `STRICT` mode (established by existing migrations)
    - Index: `(tracker_id, manga_id)` for efficient lookups by tracker + manga

    **`tracker_auth` table** (per D-04):
    - `id INTEGER PRIMARY KEY AUTOINCREMENT`
    - `tracker_id TEXT NOT NULL UNIQUE`
    - `token_json TEXT NOT NULL`
    - `expires_at TEXT`
    - `created_at TEXT NOT NULL DEFAULT (datetime('now'))`
    - Use `STRICT` mode
    - Index: `(state)` for PKCE lookups — wait, `state` is not in this table. The index `(state)` from D-04 refers to the in-memory PKCE map key, not a DB index. Omit DB index on `(state)`.
    - Index: `(tracker_id)` for auth token lookups (natural query path).

    **SQL style** matching existing migrations:
    - Capitalized SQL keywords (CREATE, TABLE, NOT NULL, etc.)
    - Line comments with `--` for each table
    - Column definitions one per line with indentation
    - Trailing `) STRICT;`
  </action>
  <verify>
    <automated>sqlx migration check: run `cargo sqlx migrate info --source backend/shared/migrations 2>&1 | head -20` (if sqlx-cli installed) or verify file parses with `sqlite3 :memory: < backend/shared/migrations/20260628000001_create_tracking_tables.sql`</automated>
  </verify>
  <done>
    Migration file creates both tables. `sqlite3 :memory: < migration.sql` executes without error. Tables exist with correct columns, UNIQUE constraints, and indices.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create shared::track::types domain types</name>
  <files>
    backend/shared/src/track/mod.rs
    backend/shared/src/track/types.rs
    backend/shared/src/lib.rs
  </files>
  <action>
    Create `backend/shared/src/track/mod.rs` re-exporting `pub mod types;` and `pub use types;`. Create `backend/shared/src/track/types.rs` with the following types.

    Follow the existing newtype pattern in `backend/shared/src/model.rs` (e.g., `SourceId` with `#[serde(transparent)]`, `#[derive(Clone, Eq, PartialEq, Hash, Deserialize, Debug, Serialize)]`)

    **TrackerService enum** (serde, Clone, Copy, Debug, PartialEq, Eq):
    - `AniList` — serialized as `"anilist"`
    - `MyAnimeList` — serialized as `"myanimelist"`
    - Use `#[serde(rename_all = "snake_case")]` or explicit `#[serde(rename = "...")]`.
    - Derive `strum::Display`/`strum::EnumString` if convenient, otherwise manual impl for `FromStr` / `Display`. Simpler: implement `Display` and `FromStr` manually (used in URL path routing and DB storage). The `tracker_id` TEXT column stores `"anilist"` / `"myanimelist"`.

    **TrackStatus enum** (serde, Clone, Copy, Debug, PartialEq, Eq):
    - Mihon-compatible variants per D-04: `CurrentlyReading`, `Completed`, `OnHold`, `Dropped`, `PlanToRead`, `Repeating`
    - `#[serde(rename_all = "SCREAMING_SNAKE_CASE")]` maps to `CURRENTLY_READING`, `COMPLETED`, etc. which matches the TEXT storage.
    - Implement conversion methods: `fn from_mihon_status(s: &str) -> Option<TrackStatus>`, `fn as_mihon_status(&self) -> &'static str`

    **SyncDirection enum** (serde, Clone, Copy, Debug, PartialEq, Eq):
    - Per D-04: `Push`, `Pull`, `TwoWay`
    - `#[serde(rename_all = "PascalCase")]`

    **TrackEntry struct** (serde, Clone, Debug):
    - Fields matching the `track` table columns (except `id` which is internal):
      - `manga_source_id: String`
      - `manga_id: String`
      - `tracker_id: String`
      - `remote_id: Option<String>`
      - `library_id: Option<String>`
      - `title: Option<String>`
      - `last_chapter_read: i32`
      - `total_chapters: Option<i32>`
      - `status: Option<TrackStatus>`
      - `score: Option<i32>`
      - `start_date: Option<String>`
      - `finish_date: Option<String>`
      - `tracking_url: Option<String>`
      - `private: bool`
      - `updated_at: String`
    - Use `#[cfg_attr(feature = "sqlx", derive(sqlx::FromRow))]` matching existing model.rs Playlist struct annotation.

    **AuthToken struct** (serde, Clone, Debug, Deserialize):
    - `access_token: String`
    - `token_type: Option<String>`
    - `refresh_token: Option<String>` — MAL has refresh tokens
    - `expires_in: Option<i64>`
    - `scope: Option<String>`
    - `created_at: Option<i64>` — epoch seconds when stored

    **PkceSessionData struct** (serde, Clone, Debug, Serialize, Deserialize):
    - `state: String` — random 32-byte hex
    - `code_verifier: String`
    - `tracker_id: String`

    Edit `backend/shared/src/lib.rs` to add `pub mod track;` before the `#[cfg(feature = "all")]` gate or alongside the existing module declarations. Since `track` module has no database/usecases dependencies, it does NOT need to be gated behind `#[cfg(feature = "all")]`.

    NO new Cargo.toml dependencies needed for shared crate — serde, chrono, sqlx, and num_enum are already present. The track/types.rs uses only serde derives and std types.
  </action>
  <verify>
    <automated>`cd backend && cargo check -p shared 2>&1 | grep -E "^error|^warning:" | head -5`</automated>
  </verify>
  <done>
    `cargo check -p shared` passes. `shared::track::types::TrackerService`, `TrackEntry`, `TrackStatus`, `SyncDirection`, `AuthToken`, `PkceSessionData` are all accessible. Types derive serde Serialize/Deserialize. `lib.rs` has `pub mod track;` declaration.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create TrackState + PkceSession in server state</name>
  <files>
    backend/server/src/track/state.rs
    backend/server/src/state.rs
  </files>
  <action>
    Create `backend/server/src/track/state.rs` with two structs:

    **PkceSession** (Clone):
    - `code_verifier: String`
    - `tracker_id: String` — `"anilist"` or `"myanimelist"`
    - `created_at: std::time::Instant`

    **TrackState** (Clone):
    - `pkce_sessions: Arc<Mutex<HashMap<String, PkceSession>>>` — keyed by the 32-byte hex `state` string
    - `fn new() -> Self` — creates empty map
    - `async fn insert(&self, state: String, session: PkceSession)` — insert with cleanup check (see below)
    - `async fn get_and_remove(&self, state: &str) -> Option<PkceSession>` — atomic get+remove (PKCE codes are one-time use)
    - `async fn cleanup_expired(&self)` — removes entries older than 15 minutes
    - The insert/get_and_remove methods should call `cleanup_expired()` lazily (check and evict stale entries before each operation)

    Constants:
    - `PKCE_SESSION_TTL: Duration = Duration::from_secs(15 * 60)` — 15-minute TTL per D-07
    - Use `std::time::Instant::now()` for expiration checks

    Edit `backend/server/src/state.rs`:

    1. Add `use crate::track::state::TrackState;` import
    2. Add `pub track_state: TrackState` field to the `State` struct
    3. Implement `FromRef<State> for TrackState` — copy the pattern from the existing `JobState` implementation at lines 55-59:

    ```rust
    impl FromRef<State> for TrackState {
        fn from_ref(state: &State) -> Self {
            state.track_state.clone()
        }
    }
    ```

    Also update `backend/server/src/app.rs` `build_state()` function to initialize the TrackState. Add after the State struct construction:
    ```rust
    track_state: TrackState::new(),
    ```
    to the State struct literal (around line 158). Note: `build_state()` must be edited later in Plan 3 when routes are wired — do NOT edit `build_state()` now. The field on `State` is sufficient for compilation at this stage since `build_state()` is the only thing that sets `track_state` and it can be set to `TrackState::new()` by Plan 3. Actually, to keep compilation working after adding `track_state` field, `build_state()` MUST also set it. So edit `build_state()` now to add `track_state: TrackState::new(),` to the State struct literal at line 158.

    Do NOT create `backend/server/src/track/mod.rs` in this task — that comes in Plan 3. The `state.rs` file lives in the `track/` directory but does not need `mod.rs` to exist yet (it won't be compiled until mod.rs imports it).
  </action>
  <verify>
    <automated>`cd backend && cargo check -p server 2>&1 | grep -E "^error" | head -5`</automated>
  </verify>
  <done>
    `cargo check -p server` passes. `TrackState` is a field on `State`. `FromRef<State> for TrackState` is implemented. PKCE session operations compile. 15-minute TTL constant defined.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-memory PKCE store ↔ route handlers | PKCE state map is shared mutable state; concurrent access via Arc<Mutex<HashMap>> is safe |
| shared types ↔ server crate | Types-only boundary; no runtime risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | migration file | mitigate | `.sql` file committed to git; sqlx runs migrations in timestamp order. Verify checksums via sqlx offline mode if enabled. |
| T-01-02 | Information Disclosure | PkceSession in memory | accept | Sessions live ~15 min in server process memory. No PII stored. Server is local-only (127.0.0.1/UDS). |
| T-01-SC | Tampering | cargo add / Cargo.toml | mitigate | No new crates in this plan. Shared types only — no new dep. |
</threat_model>

<verification>
- [ ] `cd backend && cargo check -p shared` passes
- [ ] `cd backend && cargo check -p server` passes (both crates compile with new modules)
- [ ] Migration file parses correctly via `sqlite3 :memory: < file.sql`
- [ ] TrackState compiles with Arc<Mutex<HashMap<String, PkceSession>>>
- [ ] FromRef<State> for TrackState compiles
- [ ] All types derive required traits (Serialize, Deserialize, Clone, Debug)
</verification>

<success_criteria>
1. `cargo check -p shared` and `cargo check -p server` pass without errors
2. Migration file creates both `track` and `tracker_auth` tables with correct schema
3. `TrackerService`, `TrackEntry`, `TrackStatus`, `SyncDirection`, `AuthToken`, `PkceSessionData` are usable from Rust code
4. `TrackState` is a field on `State` and `FromRef<State>` is implemented
5. PKCE session has 15-minute TTL with lazy cleanup
6. All existing tests still pass (no existing tests broken)
</success_criteria>

<output>
Create `.planning/phases/01-backend-foundation-database-schema-auth-qr/01-01-SUMMARY.md` when done, documenting what was built.
</output>
